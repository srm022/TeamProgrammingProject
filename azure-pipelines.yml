# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- azure-pipelines-integration

pool:
  vmImage: 'vs2017-win2016'

variables:
  buildConfiguration: 'Release'
  clientAppDirectory: 'PZProject\ClientApp'

steps:
# - script: dotnet build --configuration $(buildConfiguration)
#   displayName: 'dotnet build $(buildConfiguration)'
- task: DotNetCoreCLI@2
  inputs:
    command: restore
    projects: '**/*.csproj'

- task: DotNetCoreCLI@2
  displayName: Build
  inputs:
    command: build
    projects: '**/*.csproj'
    arguments: '--configuration Release'

# - task: Npm@1
#   displayName: 'npm install'
#   inputs:
#     command: install
#     workingDir: $(clientAppDirectory)

# - task: Npm@1
#   inputs:
#     command: 'custom'
#     customCommand: 'run prod-build-dev'
#     workingDir: $(clientAppDirectory)

# - task: PublishBuildArtifacts@1
#   inputs:
#     PathtoPublish: '$(clientAppDirectory)/dist'
#     ArtifactName: 'app'

- task: DotNetCoreCLI@2
  inputs:
    command: publish
    publishWebProjects: True
    arguments: '--configuration $(BuildConfiguration) --output $(Build.ArtifactStagingDirectory)'
    zipAfterPublish: True

# - task: AzureRmWebAppDeployment@3
#   inputs:
#     azureSubscription: 'Azure dla studentów(b3728535-34fc-48d5-8dc0-291fae5f6121)'
#     WebAppName: 'pzproject'
#     Package: $(System.ArtifactsDirectory)/**/*.zip

# - task: AzureWebApp@1
#   inputs:
#     azureSubscription: 'Azure dla studentów(b3728535-34fc-48d5-8dc0-291fae5f6121)'
#     appType: 'webApp'
#     appName: 'pzproject'
#     package: '$(System.DefaultWorkingDirectory)/**/*.zip'
#     deploymentMethod: 'auto'